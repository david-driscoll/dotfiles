# Neo4j credentials
NEO4J_PASSWORD=changeme_strong_password

# LM Studio settings (LM Studio must be running with model loaded)
# Default LM Studio API endpoint (do not change unless you changed LM Studio port)
OPENAI_API_KEY=lm-studio
