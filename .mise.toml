[tools]
"github:microsoft/apm" = { version = "v0.13.0", bin = "apm" }
"github:eljulians/skillfile" = { version = "v1.6.2", bin = "skillfile" }

[tasks.update-agents]
description = "Update agents to the latest version"
run = [
    "cd {{config_root}}/ai && apm install --trust-transitive-mcp --target all",
    "cd {{config_root}}/agents && apm install --trust-transitive-mcp --target all",
    "apm install --global --trust-transitive-mcp --target all",
]
